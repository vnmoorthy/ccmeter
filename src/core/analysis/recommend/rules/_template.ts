// Template for a new recommendation rule. Copy, rename, register in ../index.ts.
//
//   import type { Recommendation } from "../../../types.js";
//   import type { Rule } from "../index.js";
//
//   export const myRule: Rule = (a) => {
//     // inspect `a.sessions`, `a.daily`, `a.byProject`, `a.byModel`, `a.totals`
//     // return one or more Recommendation objects when the rule fires
//     // estimate monthly savings honestly — under-promise > over-promise
//     return [];
//   };

export {};
